name: Coverity Scan master branch

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 1-5'

jobs:
  coverity:
    if: github.repository_owner == 'wolfssl'
    runs-on: ubuntu-22.04
    timeout-minutes: 5
    steps:

    - uses: actions/checkout@master
      with:
        repository: wolfssl/wolfssl
        path: wolfssl
    - name: wolfssl autogen
      working-directory: ./wolfssl
      run: ./autogen.sh
    - name: wolfssl configure
      working-directory: ./wolfssl
      run: ./configure --enable-enckeys
    - name: wolfssl make
      working-directory: ./wolfssl
      run: make
    - name: wolfssl make install
      working-directory: ./wolfssl
      run: sudo make install

    - uses: actions/checkout@v4
      with:
        ref: master

    - name: Configure wolfMQTT with enable-all M-F
      run: |
        ./autogen.sh
        ./configure --enable-all

    - name: Check secrets
      env:
        token_var: ${{ secrets.COVERITY_SCAN_TOKEN_WOLFMQTT }}
        email_var: ${{ secrets.COVERITY_SCAN_EMAIL }}
      run: |
        token_len=${#token_var}
        echo "$token_len"
        email_len=${#email_var}
        echo "$email_len"

    - name: Cache key (month-based, tool updates ~twice yearly)
      id: cov-cache-key
      run: echo "key=$(date +%Y-%m)" >> $GITHUB_OUTPUT

    - name: Cache Coverity Build Tool
      id: cov-build-cache
      uses: actions/cache@v4
      with:
        path: cov-analysis
        key: cov-build-cxx-linux64-${{ steps.cov-cache-key.outputs.key }}

    - name: Download Coverity Build Tool
      if: steps.cov-build-cache.outputs.cache-hit != 'true'
      env:
        TOKEN: ${{ secrets.COVERITY_SCAN_TOKEN_WOLFMQTT }}
      run: |
        curl https://scan.coverity.com/download/cxx/linux64 \
          --no-progress-meter \
          --output cov-analysis.tar.gz \
          --data "token=${TOKEN}&project=wolfMQTT"
        mkdir -p cov-analysis
        tar -xzf cov-analysis.tar.gz --strip 1 -C cov-analysis

    - name: Build with cov-build
      run: |
        export PATH="${PWD}/cov-analysis/bin:${PATH}"
        cov-build --dir cov-int make

    - name: Archive results
      run: tar -czvf cov-int.tgz cov-int

    - name: Submit results to Coverity Scan
      env:
        TOKEN: ${{ secrets.COVERITY_SCAN_TOKEN_WOLFMQTT }}
        EMAIL: ${{ secrets.COVERITY_SCAN_EMAIL }}
      run: |
        curl \
          --form token="${TOKEN}" \
          --form email="${EMAIL}" \
          --form file=@cov-int.tgz \
          --form version="${GITHUB_SHA}" \
          --form description="coverity-scan wolfSSL/wolfMQTT / ${GITHUB_REF}" \
          "https://scan.coverity.com/builds?project=wolfMQTT"
